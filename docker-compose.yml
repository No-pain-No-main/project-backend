
services:
  postgres-db:
    image: postgres:16-alpine
    container_name: nopainnomain-db
    environment:
      - POSTGRES_USER=Admin2026
      - POSTGRES_PASSWORD=MRJybs28
      - POSTGRES_DB=nopainnomain_db
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    restart: always

volumes:
  postgres_data: